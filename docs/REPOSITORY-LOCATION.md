# Canonical repository location

Use D:\repos\PharmaCart for all development.

Relocated by verified copy from D:\Users\Ghareeb\Documents\ChatGPT\PharmaCart because Windows prevented renaming the active folder. All 3386 original files matched by SHA-256, including Git metadata, dependencies and ignored local setup files. Contract verification and all 54 unit/contract tests passed from the new path.

The original is retained as a backup with an AGENTS.md redirect. The Codex saved project still points at the original directory; reopen D:\repos\PharmaCart in Codex to align the project workspace. No global Codex or Git configuration was edited. Git commands in a restricted account may use the per-command option -c safe.directory=D:/Repos/PharmaCart (note the filesystem capitalization).
